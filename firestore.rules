rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // deny by default; require sign-in for everything until role-based
    // rules land alongside real auth (custom claims per role/floor).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
